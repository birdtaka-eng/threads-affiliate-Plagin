# 🛡️ Antigravity 指令書 (AGENTS.md)

このファイルは、AIアシスタント（Antigravity）がこのプロジェクトで「迷子」にならず、常に最高のパフォーマンスを発揮するための絶対的なガイドラインである。

## 🎯 プロジェクトの目的
タカ様の「異常な偏愛」を Threads へと昇華させるための、最高品質の投稿生成システム。

## 📂 フォルダの歩き方
- **BRAIN/**: GAS側のロジック。番号順（00-30）に読み込むことで、実行順序と依存関係をAIが即座に理解できる。
- **SCOUT/**: 拡張機能側。画像圧縮と通信の安定性を最優先する。
- **LABORATORY/**: 実験場。本番環境を汚さずに検証を行う。

## ⚔️ 開発の掟 (Coding Rules)
1. **迷わず聞け**: 仕様が不明瞭な場合、思い込みで進めずタカ様に確認せよ。
2. **フラットに保て**: 階層をこれ以上深くせず、機能を横に並べる「垂直スライス」を維持せよ。
3. **一本刀を折るな**: `00_Main.js` がすべての起点であり、多重定義は死を意味する。
4. **アトミック（原子単位）開発**: 一つの「ブロック（一片）」ごとに作成・検証せよ。

## 🚧 境界線の掟 (The Great Divide)
- **BRAIN (GAS)**: `Google Apps Script` 環境。
  - `document` や `window` は使えない。
  - `SpreadsheetApp` や `UrlFetchApp` が武器。
  - 役割：データの永続化、AIとの高度な対話。
- **SCOUT (Extension)**: `Web Browser` 環境。
  - `Utilities.base64Encode` などの GAS 専用命令は使えない。
  - `document.querySelector` や `canvas` が武器。
  - 役割：DOMからの情報抽出、画像圧縮、UI制御。
- **禁止事項**: BRAIN のコードを SCOUT にコピペすること、またその逆も厳禁。

## 🚀 更新の儀式
`clasp push` を行う際は、必ず以下の手順を守ること：
1. `clasp status` で差分を確認。
2. ブラウザ側のファイル名と衝突がないかチェック。
3. 推奨されるファイル構成（00-30）以外の「幽霊ファイル」は徹底的に排除。

## 🛡️ Cloudecode (安全装置) の掟
AIが独断で暴走し、タカ様の「聖域」を破壊することを防ぐための絶対的な規律。

1. **一点集中の原則**:
   一度の作業（ターン）で編集・操作するのは、**現在ターゲットとしているファイル 1つだけ**とする。
2. **余計な親切の禁止**:
   「ついでに」「気を利かせて」他のファイルやフォルダ構成をいじることを**厳禁**とする。
3. **確認の絶対義務化**:
   ファイルの作成・移動・削除、および重要なコードの書き換えを行う前には、必ず「実装計画書」を提出し、タカ様の明示的な「OK」を得ること。
4. **試行・調査・復元のループ**:
   実装が意図通りに動かない場合、独断で修正を重ねず、即座に「一つ戻す（Revert）」。その後、WEB検索等で正解を調査し、タカ様に再提案すること。
