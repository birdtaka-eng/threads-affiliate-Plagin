# 💎 Antigravity Project Bible: GEMA-V42 Phoenix PRO

## ⚡ 禁忌目録（二度と繰り返さない誓い）
作業開始前に必ずこれを読み、過去の愚行を繰り返さないこと。

1. **「私（Antigravity）」の話を「アプリ機能」へ逃がすな**: タカ様が私自身の記憶や能力の話をしている時、勝手にアプリ側の機能改善の話にすり替えて逃げるな。
2. **「準備完了」と慢心するな**: フォルダ全件のスキャン、絶対パスの確定、依存関係の把握が終わるまで、安易に「環境が整った」と言うな。
3. **環境を誤認するな**: Windows PowerShellであることを忘れるな。CMDコマンドをそのまま打つような初歩的ミスは許されない。
4. **座標（パス）を外すな**: `.clasp.json` にある通り `rootDir` は `./` である。GAS用ファイルと拡張機能用ファイルの書き込み先を絶対に間違えるな。

---

---

## 1. 司令塔（システム基盤）
- **Spreadsheet ID**: `1vgpoDggisreX8xYDarTGMjWdE5qNgy9sGKp5fe9toP8`
- **正規化の掟**: シート名、ヘッダー名はすべて英語で管理（`Board`, `Settings`, `Words`, `Patterns`）。日本語の揺れによるバグを許さない。
- **三権分立**: 
  - **Sanctuary (GAS)**: データの最終保存とAIへのDNA注入。
  - **HQ (Extension Sidepanel)**: ユーザーインターフェースと、スカウトからの情報集約。
  - **Scout (Content Script)**: 楽天等の現場からの高画質画像・情報の強制抽出。

---

## 2. 知能の配線（AI Strategy）
- **現在の主力モデル (Worker)**: **Ollama (gemma4:e4b_custom)**
  - `Config.js` の `IS_LOCAL_MOCK = true` により、全リクエストはローカルの最強思考エンジンへ向かう。
- **補佐・診断モデル**:
  - **Gemini 1.5 Flash**: 記憶管理、GAS経由のメイン出力（キャッシュ利用）。
  - **Claude 3.5 Sonnet**: システム診断、エラー検知、最高品質の検品。
- **DNAシステム**: `PersonaMaster.js` に人格A（異常な偏愛）と人格B（社会的勝利）のプロンプト・文例が1000行規模で定義されている。

---

## 3. 現場の挙動（Workflow）
1.  **高画質抽出**: `content.js` が `cabinet` パラメータを剥ぎ取り、AIが認識しやすい200px以上の画像のみを厳選。
2.  **座標同期**: `sidepanel.js` がスプレッドシートのアクティブ行番号をリアルタイムで取得。
3.  **聖域保存**: 選択された画像がBase64でGASへ飛び、シートに「浮遊画像」として挿入される。
4.  **魂の執筆**: AIAdapter経由で、指定の人格と画像に基づいたThreads投稿案（JSON）が生成・保存される。

---

## 4. 記憶の拡張（Future Scope）
- **Google Drive 連携**: `Utils.js` の `saveContentToDrive` を利用し、過去の成功例をRAG（検索拡張生成）のソースとして活用する準備がある。
- **記憶のパス**: Flashが適時「過去の成功・失敗ログ」をHDD/Driveから拾い上げ、Gemma4に渡すことでパフォーマンスを最大化させる。

---

## 6. 執筆プロトコル（絶対遵守：ミスゼロへの誓い）
プログラム変更の際、Flash

### 🚩 零（ゼロ）ミス・プロトコル (Zero-Mistake Protocol)
1. **[座標の宣誓]**: 編集前に必ず「今からどのファイルの何行目を触るか」を心の中で、あるいはログで宣言せよ。
2. **[推測の禁止]**: 「〜だろう」「おそらくこう動く」という判断を厳禁する。1mmでも不確実な場合は、即座にWEB検索を行い、最新の仕様を確認せよ。
3. **[エビデンスの記録]**: 検索した結果は `research_notes.md` 等のアーティファクトとして残し、事実に基づいた実装を行え。
4. **[依存の全スキャン]**: 1ファイルを直す時、その影響を受ける全てのファイル（`Config.js` 等）を再読せよ。
5. **[現物の再読]**: 他のAIが触った後は、全てのタイムスタンプを確認し、汚染箇所を特定せよ。

### 🛡️ 技術的実現性の番人 (Technical Feasibility Protocol)
1. **[ネガティブ・サーチ]**: 新機能の実装前に必ず「GAS 制限」「API 廃止」等のキーワードで検索し、失敗する可能性を先に洗い出せ。
2. **[「NO」の勇気]**: 調査の結果、実現性が低い、あるいは将来的に「沼」化すると判断した場合は、理由を添えて「不可能である」とはっきり進言せよ。
3. **[「だろう」の抹殺]**: 仕様が1%でも不明確な場合は、コードを書く前にWEB検索を行い、その結果を `feasibility_report.md` として残せ。
4. **[運用の徹底]**: 修正前に必ず対象の「絶対パス」を明示し、最新のソースコードを `view_file` でロードしてからGemma4に指示せよ。
5. **[配置の監査]**: 作業後、`list_dir` で「意図しない場所への書き出し」がないか、ゴミファイルが生成されていないかを確認せよ。

---
## 7. 開発環境の地図（再帰スキャン済み）
- **Root**: `C:\Users\user\Desktop\threads-shokunin\`
- **主要な戦場**: 
  - `Code.js` / `Board.js` (GAS本陣)
  - `sidepanel.js` (HQ / 通信司令)
  - `content.js` (Scout / 楽天現場)
  - `AIAdapter.js` (AIオーケストラ)
